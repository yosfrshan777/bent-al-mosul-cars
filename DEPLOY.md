# نشر بنت الموصل للسيارات

## Render
1. أنشئ حساب Render.
2. اربط مستودع GitHub الذي يحتوي المشروع.
3. أنشئ Web Service واختر Node.
4. Build: `npm install`
5. Start: `npm start`
6. أضف:
   - `ADMIN_EMAIL`
   - `ADMIN_PASSWORD`
   - `JWT_SECRET` (قيمة عشوائية طويلة)
7. بعد النشر تحصل على رابط `onrender.com`.

## ملاحظة مهمة
قاعدة البيانات والصور تحتاج تخزيناً دائماً في الإنتاج. لا تضع أسرار الدفع أو كلمات المرور داخل الكود أو GitHub.
