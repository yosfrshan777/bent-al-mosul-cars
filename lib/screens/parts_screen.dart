import 'package:flutter/material.dart';
import '../services/api_service.dart';
const _pink=Color(0xFFFF176F),_bg=Color(0xFF060810),_surface=Color(0xFF0E1522);
class PartsScreen extends StatefulWidget{const PartsScreen({super.key,required this.api});final ApiService api;@override State<PartsScreen> createState()=>_PartsScreenState();}
class _PartsScreenState extends State<PartsScreen>{bool loading=true;String? error;List<Map<String,dynamic>> items=[];@override void initState(){super.initState();_load();}Future<void>_load()async{setState((){loading=true;error=null;});try{final data=await widget.api.getParts();final raw=data is List?data:data is Map&&data['parts'] is List?data['parts']:const[];if(!mounted)return;setState((){items=raw.whereType<Map>().map((e)=>Map<String,dynamic>.from(e)).toList();loading=false;});}catch(e){if(mounted)setState((){loading=false;error=e is ApiException?e.message:'تعذر تحميل قطع الغيار';});}}void _price()=>showDialog(context:context,builder:(_)=>AlertDialog(backgroundColor:_surface,title:const Text('اشتراك قطع الغيار',style:TextStyle(color:Colors.white,fontWeight:FontWeight.w900)),content:const Text('15,000 د.ع شهرياً',style:TextStyle(color:_pink,fontSize:20,fontWeight:FontWeight.w900)),actions:[TextButton(onPressed:()=>Navigator.pop(context),child:const Text('إغلاق'))]));@override Widget build(BuildContext context){Widget body;if(loading)body=const Center(child:CircularProgressIndicator(color:_pink));else if(error!=null)body=Center(child:Column(mainAxisSize:MainAxisSize.min,children:[Text(error!,style:const TextStyle(color:Colors.white70)),TextButton(onPressed:_load,child:const Text('إعادة المحاولة'))]));else if(items.isEmpty)body=ListView(children:[const SizedBox(height:80),const Center(child:Text('لا توجد محلات قطع غيار معتمدة حالياً',style:TextStyle(color:Colors.white54))),const SizedBox(height:15),Center(child:FilledButton.icon(onPressed:_price,icon:const Icon(Icons.workspace_premium_rounded),label:const Text('الاشتراك — 15,000 د.ع')))]);else body=ListView.builder(padding:const EdgeInsets.all(15),itemCount:items.length,itemBuilder:(_,i){final x=items[i];return Container(margin:const EdgeInsets.only(bottom:12),decoration:BoxDecoration(color:_surface,borderRadius:BorderRadius.circular(20),border:Border.all(color:_pink.withOpacity(.25))),child:ListTile(onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>PartStoreDetailsScreen(api:widget.api,id:int.tryParse('${x['id']}')??0))),leading:const Icon(Icons.handyman_rounded,color:_pink,size:32),title:Text('${x['name']??'محل قطع غيار'}',style:const TextStyle(color:Colors.white,fontWeight:FontWeight.w900)),subtitle:Text('📍 ${x['city']??''}\n☎ ${x['phone']??''}',style:const TextStyle(color:Colors.white54,height:1.5)),trailing:const Icon(Icons.chevron_left_rounded,color:Colors.white38)));});return Directionality(textDirection:TextDirection.rtl,child:Scaffold(backgroundColor:_bg,appBar:AppBar(title:const Text('قطع الغيار',style:TextStyle(fontWeight:FontWeight.w900)),backgroundColor:_bg,actions:[IconButton(onPressed:_price,icon:const Icon(Icons.workspace_premium_rounded,color:_pink))]),body:RefreshIndicator(color:_pink,onRefresh:_load,child:body)));}}
class PartStoreDetailsScreen extends StatefulWidget{const PartStoreDetailsScreen({super.key,required this.api,required this.id});final ApiService api;final int id;@override State<PartStoreDetailsScreen> createState()=>_PartStoreDetailsScreenState();}
class _PartStoreDetailsScreenState extends State<PartStoreDetailsScreen>{Map<String,dynamic>? store;bool loading=true;String? error;@override void initState(){super.initState();_load();}Future<void>_load()async{try{final d=await widget.api.getPartStore(widget.id);if(!mounted)return;setState((){store=d is Map?Map<String,dynamic>.from(d):null;loading=false;error=null;});}catch(e){if(mounted)setState((){loading=false;error=e is ApiException?e.message:'تعذر تحميل بيانات محل قطع الغيار';});}}@override Widget build(BuildContext context){final name=store?['name']?.toString()??'محل قطع غيار',city=store?['city']?.toString()??'',phone=store?['phone']?.toString()??'';return Directionality(textDirection:TextDirection.rtl,child:Scaffold(backgroundColor:_bg,appBar:AppBar(title:const Text('محل قطع الغيار',style:TextStyle(fontWeight:FontWeight.w900)),backgroundColor:_bg),body:loading?const Center(child:CircularProgressIndicator(color:_pink)):error!=null?Center(child:Text(error!,style:const TextStyle(color:Colors.white54))):ListView(padding:const EdgeInsets.all(15),children:[Container(padding:const EdgeInsets.all(20),decoration:BoxDecoration(color:_surface,borderRadius:BorderRadius.circular(22),border:Border.all(color:_pink.withOpacity(.35))),child:Column(children:[const Icon(Icons.handyman_rounded,color:_pink,size:50),const SizedBox(height:10),Text(name,style:const TextStyle(color:Colors.white,fontSize:22,fontWeight:FontWeight.w900)),const SizedBox(height:8),Text('📍 $city',style:const TextStyle(color:Colors.white60)),Text('☎ $phone',style:const TextStyle(color:Colors.white60)),const SizedBox(height:14),const Text('الاشتراك: 15,000 د.ع شهرياً',style:TextStyle(color:_pink,fontWeight:FontWeight.w900)),const SizedBox(height:10),const Text('المنتجات والأسعار والتوصيل تظهر من بيانات السيرفر.',style:TextStyle(color:Colors.white54))]))]));}}
