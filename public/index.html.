<!doctype html>
<html lang="ar" dir="rtl">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="theme-color" content="#ff176f">
<title>بنت الموصل للسيارات</title>

<style>
*{box-sizing:border-box}

body{
  margin:0;
  background:#08080b;
  color:#fff;
  font-family:Tahoma,Arial,sans-serif;
}

button,input,select,textarea{
  font:inherit;
}

button{
  cursor:pointer;
}

header{
  position:sticky;
  top:0;
  z-index:50;
  background:#101015;
  border-bottom:1px solid #292933;
  padding:14px;
}

.header{
  max-width:1100px;
  margin:auto;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;
}

.logo{
  font-size:22px;
  font-weight:900;
  color:#ff176f;
}

.logo span{
  color:#fff;
}

.nav{
  display:flex;
  gap:7px;
  flex-wrap:wrap;
}

.btn{
  border:0;
  border-radius:12px;
  padding:10px 15px;
  background:#24242d;
  color:#fff;
  font-weight:700;
}

.btn:hover{
  opacity:.85;
}

.btn.primary{
  background:#ff176f;
}

.btn.green{
  background:#0e9f6e;
}

.btn.red{
  background:#c62828;
}

.container{
  max-width:1100px;
  margin:auto;
  padding:20px 14px 60px;
}

.hero{
  background:linear-gradient(135deg,#1a0c15,#111118);
  border:1px solid #30202a;
  border-radius:22px;
  padding:30px 20px;
  margin-bottom:20px;
}

.hero h1{
  margin:0 0 10px;
  font-size:32px;
}

.hero p{
  color:#bbb;
  line-height:1.8;
}

.section{
  background:#111116;
  border:1px solid #292932;
  border-radius:18px;
  padding:18px;
  margin-bottom:20px;
}

.section h2{
  margin-top:0;
}

.hidden{
  display:none!important;
}

.form{
  display:grid;
  gap:12px;
}

.grid2{
  display:grid;
  grid-template-columns:repeat(2,1fr);
  gap:12px;
}

.grid3{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:12px;
}

input,select,textarea{
  width:100%;
  background:#08080b;
  border:1px solid #34343e;
  color:#fff;
  border-radius:11px;
  padding:12px;
  outline:none;
}

textarea{
  min-height:110px;
  resize:vertical;
}

label{
  display:block;
  color:#bbb;
  font-size:14px;
  margin-bottom:5px;
}

.cars{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:15px;
}

.car{
  background:#15151b;
  border:1px solid #2b2b35;
  border-radius:17px;
  overflow:hidden;
}

.car img{
  width:100%;
  height:190px;
  object-fit:cover;
  display:block;
  background:#08080b;
}

.car-body{
  padding:14px;
}

.car-title{
  font-size:19px;
  font-weight:900;
}

.price{
  color:#ff176f;
  font-size:20px;
  font-weight:900;
  margin:8px 0;
}

.meta{
  color:#aaa;
  font-size:13px;
  line-height:1.8;
}

.actions{
  display:flex;
  flex-wrap:wrap;
  gap:7px;
  margin-top:12px;
}

.notice{
  padding:13px;
  border-radius:12px;
  background:#1d1820;
  border:1px solid #3b2935;
  color:#ddd;
  line-height:1.8;
}

.notice strong{
  color:#ff176f;
}

.status{
  padding:6px 9px;
  border-radius:8px;
  display:inline-block;
  font-size:12px;
  background:#292933;
}

.status.published,
.status.approved{
  background:#0e704f;
}

.status.pending{
  background:#765b12;
}

.status.rejected{
  background:#762020;
}

table{
  width:100%;
  border-collapse:collapse;
  min-width:700px;
}

th,td{
  padding:10px;
  border-bottom:1px solid #292932;
  text-align:right;
}

.table-wrap{
  overflow:auto;
}

.empty{
  text-align:center;
  color:#999;
  padding:30px;
}

.footer{
  text-align:center;
  color:#777;
  padding:30px 10px;
}

.small{
  color:#999;
  font-size:12px;
}

@media(max-width:800px){
  .cars{
    grid-template-columns:repeat(2,1fr);
  }

  .grid3{
    grid-template-columns:1fr 1fr;
  }
}

@media(max-width:600px){
  .header{
    align-items:flex-start;
    flex-direction:column;
  }

  .nav{
    width:100%;
  }

  .nav .btn{
    flex:1;
  }

  .cars,
  .grid2,
  .grid3{
    grid-template-columns:1fr;
  }

  .hero h1{
    font-size:25px;
  }

  .car img{
    height:210px;
  }
}
</style>
</head>

<body>

<header>
  <div class="header">

    <div class="logo">
      🚗 بنت <span>الموصل للسيارات</span>
    </div>

    <div class="nav">
      <button class="btn" onclick="showPage('home')">الرئيسية</button>
      <button class="btn" onclick="showPage('cars')">السيارات</button>
      <button class="btn" onclick="showPage('add')">بيع سيارتك</button>
      <button class="btn" id="loginBtn" onclick="showPage('login')">دخول</button>
      <button class="btn hidden" id="accountBtn" onclick="showPage('account')">حسابي</button>
      <button class="btn hidden" id="adminBtn" onclick="showPage('admin')">الإدارة</button>
      <button class="btn red hidden" id="logoutBtn" onclick="logout()">خروج</button>
    </div>

  </div>
</header>

<main class="container">

<!-- HOME -->
<section id="page-home">

  <div class="hero">
    <h1>🚘 بنت الموصل للسيارات</h1>

    <p>
      منصة بيع وشراء السيارات في الموصل والعراق.
      انشر سيارتك وخلي إعلانك يوصل للمشترين.
    </p>

    <div class="actions">
      <button class="btn primary" onclick="showPage('cars')">
        🔎 تصفح السيارات
      </button>

      <button class="btn" onclick="showPage('add')">
        ➕ أضف سيارتك
      </button>
    </div>
  </div>

  <div class="section">
    <h2>🔥 أحدث السيارات</h2>
    <div id="latestCars" class="cars">
      <div class="empty">جاري تحميل السيارات...</div>
    </div>
  </div>

</section>

<!-- CARS -->
<section id="page-cars" class="hidden">

  <div class="section">

    <h2>🔎 البحث عن سيارة</h2>

    <div class="grid3">

      <div>
        <label>بحث</label>
        <input id="search" placeholder="تويوتا، كامري، موصل...">
      </div>

      <div>
        <label>المحافظة / المدينة</label>
        <input id="city" placeholder="الموصل">
      </div>

      <div>
        <label>الماركة</label>
        <input id="brand" placeholder="Toyota">
      </div>

      <div>
        <label>أقل سعر</label>
        <input id="minPrice" type="number" placeholder="0">
      </div>

      <div>
        <label>أعلى سعر</label>
        <input id="maxPrice" type="number" placeholder="100000000">
      </div>

    </div>

    <div class="actions">
      <button class="btn primary" onclick="loadCars()">بحث</button>
      <button class="btn" onclick="clearSearch()">مسح</button>
    </div>

  </div>

  <div class="section">
    <h2>🚗 السيارات</h2>
    <div id="carsList" class="cars"></div>
  </div>

</section>

<!-- LOGIN -->
<section id="page-login" class="hidden">

  <div class="section">

    <h2>🔐 تسجيل الدخول</h2>

    <form class="form" onsubmit="login(event)">

      <div>
        <label>رقم الهاتف</label>
        <input id="loginPhone" required>
      </div>

      <div>
        <label>كلمة المرور</label>
        <input id="loginPassword" type="password" required>
      </div>

      <button class="btn primary" type="submit">
        دخول
      </button>

    </form>

    <p class="small">
      ما عندك حساب؟
      <button class="btn" onclick="showPage('register')">
        إنشاء حساب
      </button>
    </p>

  </div>

</section>

<!-- REGISTER -->
<section id="page-register" class="hidden">

  <div class="section">

    <h2>📝 إنشاء حساب</h2>

    <form class="form" onsubmit="register(event)">

      <div>
        <label>الاسم</label>
        <input id="regName" required minlength="2">
      </div>

      <div>
        <label>رقم الهاتف</label>
        <input id="regPhone" required>
      </div>

      <div>
        <label>البريد الإلكتروني - اختياري</label>
        <input id="regEmail" type="email">
      </div>

      <div>
        <label>كلمة المرور</label>
        <input id="regPassword" type="password" required minlength="6">
      </div>

      <button class="btn primary" type="submit">
        إنشاء الحساب
      </button>

    </form>

  </div>

</section>

<!-- ADD CAR -->
<section id="page-add" class="hidden">

  <div class="section">

    <h2>🚘 إضافة سيارة للبيع</h2>

    <div class="notice">
      <strong>تنبيه:</strong>
      بعد إضافة السيارة تظهر بحالة مراجعة، وبعد الموافقة من الإدارة
      يتم نشر الإعلان.
    </div>

    <br>

    <form class="form" onsubmit="addCar(event)">

      <div class="grid2">

        <div>
          <label>الماركة</label>
          <input id="carBrand" required placeholder="Toyota">
        </div>

        <div>
          <label>الموديل</label>
          <input id="carModel" required placeholder="Camry">
        </div>

        <div>
          <label>السنة</label>
          <input id="carYear" type="number" required>
        </div>

        <div>
          <label>السعر بالدينار العراقي</label>
          <input id="carPrice" type="number" required>
        </div>

        <div>
          <label>الكيلومترات</label>
          <input id="carKm" type="number" value="0">
        </div>

        <div>
          <label>المدينة</label>
          <input id="carCity" required placeholder="الموصل">
        </div>

        <div>
          <label>الوقود</label>
          <select id="carFuel">
            <option value="">اختياري</option>
            <option>بنزين</option>
            <option>ديزل</option>
            <option>هايبرد</option>
            <option>كهرباء</option>
          </select>
        </div>

        <div>
          <label>ناقل الحركة</label>
          <select id="carTransmission">
            <option value="">اختياري</option>
            <option>أوتوماتيك</option>
            <option>عادي</option>
          </select>
        </div>

      </div>

      <div>
        <label>الوصف</label>
        <textarea id="carDescription" placeholder="اكتب تفاصيل السيارة..."></textarea>
      </div>

      <div>
        <label>الخطة</label>
        <select id="carPlan">
          <option value="10000">عادي - 10,000 د.ع</option>
          <option value="20000">مميز - 20,000 د.ع</option>
          <option value="30000">VIP - 30,000 د.ع</option>
        </select>
      </div>

      <div>
        <label>صور السيارة - حتى 5 صور</label>
        <input id="carImages" type="file" accept="image/jpeg,image/png,image/webp" multiple>
      </div>

      <button class="btn primary" type="submit">
        نشر السيارة
      </button>

    </form>

  </div>

</section>

<!-- PAYMENT -->
<section id="page-payment" class="hidden">

  <div class="section">

    <h2>💳 إرسال إثبات الدفع</h2>

    <div class="notice">
      بعد إرسال السيارة، اختر الإعلان وأرسل مبلغ الخطة مع صورة الإيصال.
      <br>
      <strong>10,000 د.ع</strong> عادي —
      <strong>20,000 د.ع</strong> مميز —
      <strong>30,000 د.ع</strong> VIP
    </div>

    <br>

    <form class="form" onsubmit="sendPayment(event)">

      <div>
        <label>رقم الإعلان</label>
        <input id="paymentCarId" type="number" required>
      </div>

      <div>
        <label>المبلغ</label>
        <select id="paymentAmount">
          <option value="10000">10,000 د.ع</option>
          <option value="20000">20,000 د.ع</option>
          <option value="30000">30,000 د.ع</option>
        </select>
      </div>

      <div>
        <label>رقم العملية / المرجع</label>
        <input id="paymentReference">
      </div>

      <div>
        <label>صورة الإيصال</label>
        <input id="paymentReceipt" type="file" accept="image/jpeg,image/png,image/webp" required>
      </div>

      <button class="btn primary" type="submit">
        إرسال الدفع
      </button>

    </form>

  </div>

</section>

<!-- ACCOUNT -->
<section id="page-account" class="hidden">

  <div class="section">

    <h2>👤 حسابي</h2>

    <div id="userInfo"></div>

  </div>

  <div class="section">

    <h2>🚗 إعلاناتي</h2>

    <div id="myCars"></div>

  </div>

  <div class="section">

    <h2>💳 مدفوعاتي</h2>

    <div id="myPayments"></div>

  </div>

</section>

<!-- ADMIN -->
<section id="page-admin" class="hidden">

  <div class="section">

    <h2>👑 لوحة الإدارة</h2>

    <div id="adminCars"></div>

  </div>

  <div class="section">

    <h2>💳 المدفوعات</h2>

    <div id="adminPayments"></div>

  </div>

  <div class="section">

    <h2>👥 المستخدمون</h2>

    <div id="adminUsers"></div>

  </div>

</section>

<div id="toast"></div>

<div class="footer">
  © 2026 بنت الموصل للسيارات
</div>

</main>

<script>

const API = "/api";

let token = localStorage.getItem("bam_token") || "";
let currentUser = null;

/* ================================
   HELPERS
================================ */

function esc(value){
  return String(value ?? "")
    .replaceAll("&","&amp;")
    .replaceAll("<","&lt;")
    .replaceAll(">","&gt;")
    .replaceAll('"',"&quot;")
    .replaceAll("'","&#039;");
}

function money(value){
  return Number(value || 0).toLocaleString("ar-IQ") + " د.ع";
}

function toast(message){
  alert(message);
}

function headers(json=true){
  const h = {};

  if(json){
    h["Content-Type"] = "application/json";
  }

  if(token){
    h.Authorization = "Bearer " + token;
  }

  return h;
}

async function api(path, options={}){
  const response = await fetch(API + path, {
    ...options,
    headers:{
      ...headers(!(options.body instanceof FormData)),
      ...(options.headers || {})
    }
  });

  let data = {};

  try{
    data = await response.json();
  }catch(e){}

  if(response.status === 401){
    logout(false);
  }

  if(!response.ok){
    throw new Error(
      data.error ||
      "حدث خطأ في الطلب"
    );
  }

  return data;
}

/* ================================
   PAGE
================================ */

const pages = [
  "home",
  "cars",
  "login",
  "register",
  "add",
  "payment",
  "account",
  "admin"
];

function showPage(page){

  pages.forEach(p => {
    const el = document.getElementById("page-" + p);

    if(el){
      el.classList.toggle(
        "hidden",
        p !== page
      );
    }
  });

  window.scrollTo({
    top:0,
    behavior:"smooth"
  });

  if(page === "cars"){
    loadCars();
  }

  if(page === "account"){
    loadAccount();
  }

  if(page === "admin"){
    loadAdmin();
  }
}

/* ================================
   AUTH UI
================================ */

function updateAuthUI(){

  const logged = !!token;

  document
    .getElementById("loginBtn")
    .classList.toggle("hidden",logged);

  document
    .getElementById("accountBtn")
    .classList.toggle("hidden",!logged);

  document
    .getElementById("logoutBtn")
    .classList.toggle("hidden",!logged);

  document
    .getElementById("adminBtn")
    .classList.toggle(
      "hidden",
      !logged || !currentUser || currentUser.role !== "admin"
    );
}

/* ================================
   REGISTER
================================ */

async function register(event){

  event.preventDefault();

  try{

    const body = {
      name: document.getElementById("regName").value.trim(),
      phone: document.getElementById("regPhone").value.trim(),
      email: document.getElementById("regEmail").value.trim(),
      password: document.getElementById("regPassword").value
    };

    const data = await api("/register",{
      method:"POST",
      body:JSON.stringify(body)
    });

    token = data.token;
    currentUser = data.user;

    localStorage.setItem(
      "bam_token",
      token
    );

    updateAuthUI();

    toast("تم إنشاء الحساب بنجاح");

    showPage("account");

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   LOGIN
================================ */

async function login(event){

  event.preventDefault();

  try{

    const body = {
      phone: document.getElementById("loginPhone").value.trim(),
      password: document.getElementById("loginPassword").value
    };

    const data = await api("/login",{
      method:"POST",
      body:JSON.stringify(body)
    });

    token = data.token;
    currentUser = data.user;

    localStorage.setItem(
      "bam_token",
      token
    );

    updateAuthUI();

    toast("تم تسجيل الدخول");

    showPage(
      currentUser.role === "admin"
        ? "admin"
        : "account"
    );

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   LOGOUT
================================ */

function logout(show=true){

  token = "";
  currentUser = null;

  localStorage.removeItem("bam_token");

  updateAuthUI();

  if(show){
    toast("تم تسجيل الخروج");
  }

  showPage("home");
}

/* ================================
   ME
================================ */

async function loadMe(){

  if(!token){
    updateAuthUI();
    return;
  }

  try{

    const data = await api("/me");

    currentUser = data.user;

    updateAuthUI();

  }catch(error){

    token = "";
    currentUser = null;

    localStorage.removeItem("bam_token");

    updateAuthUI();

  }
}

/* ================================
   CARS
================================ */

function carCard(car,admin=false){

  const image = car.image
    ? esc(car.image)
    : "";

  const img = image
    ? `<img src="${image}" alt="${esc(car.brand)} ${esc(car.model)}">`
    : `<div style="height:190px;display:flex;align-items:center;justify-content:center;font-size:55px;background:#08080b">🚗</div>`;

  let buttons = "";

  if(admin){

    buttons = `
      <div class="actions">
        ${
          car.status !== "published"
          ? `<button class="btn green" onclick="publishCar(${car.id})">نشر</button>`
          : ""
        }

        ${
          car.status !== "rejected"
          ? `<button class="btn red" onclick="rejectCar(${car.id})">رفض</button>`
          : ""
        }

        <button class="btn red" onclick="deleteCarAdmin(${car.id})">
          حذف
        </button>
      </div>
    `;

  }else{

    buttons = `
      <div class="actions">

        <button class="btn primary"
          onclick="showPaymentFor(${car.id},${car.plan})">
          💳 دفع الإعلان
        </button>

      </div>
    `;

  }

  return `
    <article class="car">

      ${img}

      <div class="car-body">

        <div class="car-title">
          ${esc(car.brand)} ${esc(car.model)}
        </div>

        <div class="price">
          ${money(car.price)}
        </div>

        <div class="meta">
          📅 ${esc(car.year)}
          <br>
          📍 ${esc(car.city)}
          <br>
          🛣️ ${Number(car.km || 0).toLocaleString("ar-IQ")} كم
          <br>
          ⛽ ${esc(car.fuel || "غير محدد")}
          <br>
          ⚙️ ${esc(car.transmission || "غير محدد")}
        </div>

        <p>
          ${esc(car.description || "")}
        </p>

        ${
          car.seller_name
          ? `
            <div class="small">
              البائع: ${esc(car.seller_name)}
              <br>
              📞 ${esc(car.seller_phone || "")}
            </div>
          `
          : ""
        }

        ${
          car.status
          ? `
            <div style="margin-top:8px">
              <span class="status ${esc(car.status)}">
                ${statusText(car.status)}
              </span>
            </div>
          `
          : ""
        }

        ${buttons}

      </div>

    </article>
  `;
}

function statusText(status){

  const map = {
    pending:"قيد المراجعة",
    published:"منشور",
    rejected:"مرفوض",
    approved:"مقبول"
  };

  return map[status] || status;
}

async function loadCars(){

  const box = document.getElementById("carsList");

  if(!box) return;

  box.innerHTML =
    `<div class="empty">جاري التحميل...</div>`;

  try{

    const params = new URLSearchParams();

    const search =
      document.getElementById("search")?.value.trim() || "";

    const city =
      document.getElementById("city")?.value.trim() || "";

    const brand =
      document.getElementById("brand")?.value.trim() || "";

    const minPrice =
      document.getElementById("minPrice")?.value || "";

    const maxPrice =
      document.getElementById("maxPrice")?.value || "";

    if(search) params.set("search",search);
    if(city) params.set("city",city);
    if(brand) params.set("brand",brand);
    if(minPrice) params.set("minPrice",minPrice);
    if(maxPrice) params.set("maxPrice",maxPrice);

    const data =
      await api("/cars?" + params.toString());

    if(!data.cars.length){

      box.innerHTML =
        `<div class="empty">ماكو سيارات منشورة حالياً.</div>`;

      return;
    }

    box.innerHTML =
      data.cars.map(carCard).join("");

  }catch(error){

    box.innerHTML =
      `<div class="empty">${esc(error.message)}</div>`;

  }
}

async function loadLatest(){

  try{

    const data =
      await api("/cars");

    const box =
      document.getElementById("latestCars");

    if(!data.cars.length){

      box.innerHTML =
        `<div class="empty">ماكو سيارات منشورة حالياً.</div>`;

      return;
    }

    box.innerHTML =
      data.cars
        .slice(0,6)
        .map(carCard)
        .join("");

  }catch(error){

    document.getElementById("latestCars").innerHTML =
      `<div class="empty">${esc(error.message)}</div>`;

  }
}

function clearSearch(){

  ["search","city","brand","minPrice","maxPrice"]
    .forEach(id => {
      const el = document.getElementById(id);
      if(el) el.value = "";
    });

  loadCars();
}

/* ================================
   ADD CAR
================================ */

async function addCar(event){

  event.preventDefault();

  if(!token){

    toast("لازم تسجل دخول أولاً");

    showPage("login");

    return;
  }

  try{

    const form =
      new FormData();

    form.append(
      "brand",
      document.getElementById("carBrand").value.trim()
    );

    form.append(
      "model",
      document.getElementById("carModel").value.trim()
    );

    form.append(
      "year",
      document.getElementById("carYear").value
    );

    form.append(
      "price",
      document.getElementById("carPrice").value
    );

    form.append(
      "km",
      document.getElementById("carKm").value || "0"
    );

    form.append(
      "city",
      document.getElementById("carCity").value.trim()
    );

    form.append(
      "fuel",
      document.getElementById("carFuel").value
    );

    form.append(
      "transmission",
      document.getElementById("carTransmission").value
    );

    form.append(
      "description",
      document.getElementById("carDescription").value.trim()
    );

    form.append(
      "plan",
      document.getElementById("carPlan").value
    );

    const files =
      document.getElementById("carImages").files;

    for(let i=0;i<files.length && i<5;i++){

      form.append(
        "images",
        files[i]
      );

    }

    const data =
      await api("/cars",{
        method:"POST",
        body:form
      });

    toast(
      data.message ||
      "تمت إضافة السيارة"
    );

    document.querySelector(
      "#page-add form"
    ).reset();

    showPaymentFor(
      data.car.id,
      data.car.plan
    );

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   PAYMENT
================================ */

function showPaymentFor(carId,plan){

  document.getElementById("paymentCarId").value =
    carId;

  document.getElementById("paymentAmount").value =
    String(plan || 10000);

  showPage("payment");
}

async function sendPayment(event){

  event.preventDefault();

  if(!token){

    toast("لازم تسجل دخول");

    showPage("login");

    return;
  }

  try{

    const form =
      new FormData();

    form.append(
      "car_id",
      document.getElementById("paymentCarId").value
    );

    form.append(
      "amount",
      document.getElementById("paymentAmount").value
    );

    form.append(
      "reference",
      document.getElementById("paymentReference").value.trim()
    );

    const file =
      document.getElementById("paymentReceipt").files[0];

    if(file){
      form.append("receipt",file);
    }

    const data =
      await api("/payments",{
        method:"POST",
        body:form
      });

    toast(data.message);

    document.querySelector(
      "#page-payment form"
    ).reset();

    showPage("account");

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   ACCOUNT
================================ */

async function loadAccount(){

  if(!token){

    showPage("login");

    return;
  }

  try{

    const me =
      await api("/me");

    currentUser =
      me.user;

    document.getElementById("userInfo").innerHTML = `
      <div class="notice">
        <strong>${esc(currentUser.name)}</strong>
        <br>
        📞 ${esc(currentUser.phone)}
        <br>
        ✉️ ${esc(currentUser.email || "لا يوجد")}
        <br>
        الصلاحية:
        ${currentUser.role === "admin" ? "مدير" : "مستخدم"}
      </div>
    `;

    const cars =
      await api("/my-cars");

    const carsBox =
      document.getElementById("myCars");

    if(!cars.cars.length){

      carsBox.innerHTML =
        `<div class="empty">ما عندك إعلانات حالياً.</div>`;

    }else{

      carsBox.innerHTML = `
        <div class="cars">
          ${cars.cars.map(carCard).join("")}
        </div>
      `;

    }

    const payments =
      await api("/my-payments");

    const paymentBox =
      document.getElementById("myPayments");

    if(!payments.payments.length){

      paymentBox.innerHTML =
        `<div class="empty">ماكو مدفوعات.</div>`;

    }else{

      paymentBox.innerHTML = `
        <div class="table-wrap">
          <table>
            <thead>
              <tr>
                <th>الإعلان</th>
                <th>المبلغ</th>
                <th>الحالة</th>
                <th>المرجع</th>
              </tr>
            </thead>

            <tbody>

              ${payments.payments.map(p => `
                <tr>
                  <td>
                    ${esc(p.brand)} ${esc(p.model)}
                  </td>

                  <td>
                    ${money(p.amount)}
                  </td>

                  <td>
                    <span class="status ${esc(p.status)}">
                      ${statusText(p.status)}
                    </span>
                  </td>

                  <td>
                    ${esc(p.reference || "-")}
                  </td>
                </tr>
              `).join("")}

            </tbody>
          </table>
        </div>
      `;

    }

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   DELETE OWN CAR
================================ */

async function deleteOwnCar(id){

  if(!confirm("متأكد تريد حذف الإعلان؟")) return;

  try{

    const data =
      await api("/cars/" + id,{
        method:"DELETE"
      });

    toast(data.message);

    loadAccount();

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   ADMIN
================================ */

async function loadAdmin(){

  if(!currentUser || currentUser.role !== "admin"){

    toast("صلاحية الإدارة مطلوبة");

    showPage("home");

    return;
  }

  try{

    const cars =
      await api("/admin/cars");

    const box =
      document.getElementById("adminCars");

    if(!cars.cars.length){

      box.innerHTML =
        `<div class="empty">لا توجد إعلانات.</div>`;

    }else{

      box.innerHTML = `
        <div class="cars">
          ${cars.cars.map(c => carCard(c,true)).join("")}
        </div>
      `;

    }

    const payments =
      await api("/admin/payments");

    const pbox =
      document.getElementById("adminPayments");

    if(!payments.payments.length){

      pbox.innerHTML =
        `<div class="empty">لا توجد مدفوعات.</div>`;

    }else{

      pbox.innerHTML = `
        <div class="table-wrap">

          <table>

            <thead>
              <tr>
                <th>المستخدم</th>
                <th>السيارة</th>
                <th>المبلغ</th>
                <th>الحالة</th>
                <th>المرجع</th>
                <th>إجراء</th>
              </tr>
            </thead>

            <tbody>

              ${payments.payments.map(p => `

                <tr>

                  <td>
                    ${esc(p.user_name)}
                    <br>
                    ${esc(p.user_phone)}
                  </td>

                  <td>
                    ${esc(p.brand)}
                    ${esc(p.model)}
                  </td>

                  <td>
                    ${money(p.amount)}
                  </td>

                  <td>
                    <span class="status ${esc(p.status)}">
                      ${statusText(p.status)}
                    </span>
                  </td>

                  <td>
                    ${esc(p.reference || "-")}
                  </td>

                  <td>

                    <div class="actions">

                      ${
                        p.status === "pending"
                        ? `
                          <button class="btn green"
                            onclick="approvePayment(${p.id})">
                            قبول
                          </button>

                          <button class="btn red"
                            onclick="rejectPayment(${p.id})">
                            رفض
                          </button>
                        `
                        : ""
                      }

                    </div>

                  </td>

                </tr>

              `).join("")}

            </tbody>

          </table>

        </div>
      `;

    }

    const users =
      await api("/admin/users");

    const ubox =
      document.getElementById("adminUsers");

    ubox.innerHTML = `
      <div class="table-wrap">

        <table>

          <thead>
            <tr>
              <th>الاسم</th>
              <th>الهاتف</th>
              <th>الإيميل</th>
              <th>الصلاحية</th>
              <th>التاريخ</th>
            </tr>
          </thead>

          <tbody>

            ${users.users.map(u => `

              <tr>
                <td>${esc(u.name)}</td>
                <td>${esc(u.phone)}</td>
                <td>${esc(u.email || "-")}</td>
                <td>${esc(u.role)}</td>
                <td>${esc(u.created_at)}</td>
              </tr>

            `).join("")}

          </tbody>

        </table>

      </div>
    `;

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   ADMIN ACTIONS
================================ */

async function publishCar(id){

  try{

    const data =
      await api(
        `/admin/cars/${id}/publish`,
        {
          method:"PATCH"
        }
      );

    toast(data.message);

    loadAdmin();

  }catch(error){

    toast(error.message);

  }
}

async function rejectCar(id){

  if(!confirm("متأكد تريد رفض الإعلان؟")) return;

  try{

    const data =
      await api(
        `/admin/cars/${id}/reject`,
        {
          method:"PATCH"
        }
      );

    toast(data.message);

    loadAdmin();

  }catch(error){

    toast(error.message);

  }
}

async function deleteCarAdmin(id){

  if(!confirm("متأكد تريد حذف الإعلان نهائياً؟")) return;

  try{

    const data =
      await api(
        `/admin/cars/${id}`,
        {
          method:"DELETE"
        }
      );

    toast(data.message);

    loadAdmin();

  }catch(error){

    toast(error.message);

  }
}

async function approvePayment(id){

  if(!confirm("تأكيد قبول الدفع ونشر الإعلان؟")) return;

  try{

    const data =
      await api(
        `/admin/payments/${id}/approve`,
        {
          method:"PATCH"
        }
      );

    toast(data.message);

    loadAdmin();

  }catch(error){

    toast(error.message);

  }
}

async function rejectPayment(id){

  if(!confirm("تأكيد رفض الدفع؟")) return;

  try{

    const data =
      await api(
        `/admin/payments/${id}/reject`,
        {
          method:"PATCH"
        }
      );

    toast(data.message);

    loadAdmin();

  }catch(error){

    toast(error.message);

  }
}

/* ================================
   START
================================ */

async function start(){

  await loadMe();

  updateAuthUI();

  await loadLatest();

  showPage("home");
}

start();

</script>

</body>
</html>
